{
  "name": "lobbywatch",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:run": "vitest run"
  },
  "devDependencies": {
    "vitest": "^2.0.0"
  }
}
